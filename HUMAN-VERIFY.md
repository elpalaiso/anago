# 사람 확인 필요 — M0·M1

M0와 M1은 **코드가 완성됐지만 실서버에서 검증되지 않았습니다**. 유닛/통합
테스트(995개)는 결정 가능한 부분만 덮습니다 — 어떤 설정을 만들지, 어떤
상태 전이를 할지, 무엇을 거절하고 뭐라고 안내할지. 나머지, 즉 **커널·네트
워크·CA·다른 기계가 있어야 알 수 있는 것**은 사람이 한 번 돌려봐야 합니다.

이 문서는 그 목록입니다. 소스에서 `**Human verification needed**`로 표시한
지점(51곳)과 리뷰에서 나온 운영상 함정을 모았습니다.

**검증 환경**(DESIGN §11): Cloudflare에 등록된 개인 도메인 + 리눅스 VPS
+ 기기 2대(맥 1, 리눅스 1이면 §5의 플랫폼 차이까지 같이 확인됩니다)
+ M1은 **폰 1대**(공식 WireGuard 앱).

1~8절이 M0, 9절부터가 M1입니다. **순서대로 하세요** — M1의 합격 기준인
"폰과 기존 기기의 상호 ping"은 M0의 FORWARD 정책과 `ip_forward`가 맞아야
성립하므로, 3절을 건너뛰고 마지막 절만 볼 수는 없습니다.

---

# M0

## 1. 준비 — DNS와 인증서

- [ ] **A 레코드가 VPS 공인 IP로 해석된다**
      `dig +short net.example.com` 이 VPS의 주소를 그대로 반환하는지.
      Cloudflare라면 그 레코드가 **DNS only(회색 구름)** 인지 — 프록시가
      켜져 있으면 이름이 Cloudflare 주소로 풀리고 `51820/udp`가 전달되지
      않습니다. HTTPS는 되는데 터널만 안 되는 형태로 실패합니다.
- [ ] **`server init`이 안내한 A 레코드가 실제 주소와 일치한다**
      init은 UDP 소켓의 로컬 주소로 공인 IP를 추정합니다(패킷은 보내지
      않습니다). AWS/GCP처럼 1:1 NAT를 쓰면 사설 주소가 나오고, 그 경우
      anago는 주소를 지어내지 않고 자리표시자를 출력해야 합니다 —
      **어느 쪽이 나왔는지 확인**하세요.
- [ ] **TLS 인증서가 기기에서 신뢰된다**
      공개 신뢰 인증서(Let's Encrypt 등)면 그대로. Cloudflare Origin CA를
      썼다면 **합류할 모든 기기에 그 CA를 설치**해야 하고, 안 하면 join이
      `UnknownIssuer`로 실패합니다.

## 2. 허브 세우기

- [ ] **`sudo anago server init …`이 끝까지 간다**
      실행되는 것: `wg genkey`/`wg pubkey`, `/var/lib/anago` 0700 생성과
      `state.json` 0600, `/etc/wireguard/anago.conf` 0600, systemd 유닛
      설치·기동. 출력의 A 레코드·포트 체크리스트·조인 코드 한 줄 확인.
- [ ] **두 번째 `server init`이 거부된다** — 기존 허브를 재키잉하지 않고
      멈추는지. (`state.json`이나 `anago.conf`가 이미 있는 경우)
- [ ] **systemd 유닛이 실제로 뜬다**
      `systemctl status anago`, `journalctl -u anago`. 유닛은
      `ProtectHome=tmpfs` + `BindReadOnlyPaths=`(바이너리·인증서·키)로
      샌드박스되어 있습니다 — **인증서가 `/root`나 `/tmp`에 있는 배치도
      기동되는지**가 이 항목의 핵심입니다.
- [ ] **`--no-systemd`면 `sudo anago server run`이 포그라운드로 뜬다**
      배너에 도메인·포트·기기 수가 나오는지. :443 바인드는 root가 필요합
      니다.
- [ ] **인증서 경로가 틀렸을 때 아무것도 만들지 않고 멈춘다**
      init은 파일을 쓰기 전에 TLS를 검증합니다. 실패 후 `state.json`이
      생기지 않았는지 확인 — 생겼다면 재시도가 막힙니다.

## 3. 방화벽과 포워딩 (기기끼리 안 닿는 원인 1·2위)

- [ ] **:443/tcp가 열려 있다** — 밖에서 `nc -vz <domain> 443`. 클라우드
      보안 그룹과 호스트 방화벽 **양쪽**을 봐야 합니다.
- [ ] **:51820/udp가 열려 있다** — 이건 포트 스캔으로 확인되지 않습니다.
      `nc -vzu`는 데이터그램을 보낸 것만으로 성공을 표시하고, WireGuard는
      인증되지 않은 패킷에 아무 응답도 하지 않으므로 **허용된 경우와
      드롭된 경우가 똑같이 보입니다**. 합격 증거는 둘입니다:
      1. 규칙 자체를 눈으로 확인 — 클라우드 보안 그룹의 인바운드 UDP
         51820, 그리고 호스트 쪽 `sudo ufw status verbose` /
         `sudo iptables -S` / `sudo nft list ruleset`.
      2. **실제 핸드셰이크가 성사됨** — 기기를 붙인 뒤 서버에서
         `sudo wg show anago` 의 `latest handshake`가 갱신되는지(기기
         쪽에서도 같은 명령). 값이 계속 비어 있으면 UDP가 막힌 것입니다.
      막힌 것 같은데 원인이 애매하면 서버에서
      `sudo tcpdump -ni any udp port 51820` 를 띄워 두고 기기에서 ping을
      보내 보세요 — 패킷이 아예 안 보이면 경로/보안그룹, 들어오는데 응답이
      없으면 호스트 방화벽이나 wg 설정입니다.
- [ ] **`net.ipv4.ip_forward=1`이고 재부팅 후에도 유지된다**
      (`/etc/sysctl.d/99-anago.conf`). 꺼져 있으면 anago가 적용 후 경고를
      찍는지도 함께 확인하세요.
- [ ] **FORWARD 정책이 `anago`→`anago`를 허용한다**
      UFW/iptables/nftables 중 쓰는 것 하나로. **재부팅 후에도 남는지**가
      핵심입니다 — anago는 systemd가 되살리지만 방화벽 규칙은 저장하지
      않으면 사라집니다.

## 4. 기기 등록

- [ ] **`sudo anago code` → `sudo anago join …`이 성공한다** (기기 1)
      기기가 키쌍을 만들고 공개키만 보내는지, wg 설정이 써지고 터널이
      올라오는지, 서버 `.1` ping 확인이 통과하는지.
- [ ] **sudo로 join했는데 이후 명령은 sudo 없이 된다**
      `anago ls`가 일반 사용자로 동작해야 합니다. 확인:
      `ls -l ~/.config/anago/device.json` 소유자가 **본인**이고 0600인지.
      (`SUDO_UID` 기반 위치 결정 + 디스크립터 `fchown` 경로 — 이건 root로
      실행돼야만 도는 코드라 테스트가 덮지 못합니다.)
- [ ] **설정 디렉터리를 미리 안 만들었으면 안내하고 멈춘다**
      `mkdir -p "${XDG_CONFIG_HOME:-$HOME/.config}/anago"` 없이 join하면
      `NoConfigDir`로 멈추는지 — root가 남의 홈에 디렉터리를 만들지 않는
      설계입니다.
- [ ] **`XDG_CONFIG_HOME`을 옮겨 쓴다면 `sudo -E`로 join한 뒤 `anago ls`가
      같은 파일을 찾는다**
- [ ] **기기 2도 붙는다** — 주소가 `.3`으로 나가는지, `anago ls`(허브에서
      `sudo`)에 둘 다 보이고 마지막 핸드셰이크가 찍히는지.

## 5. 실제로 통하는지 — M0의 합격 기준

- [ ] **기기 1 ↔ 기기 2 상호 ping** (§11의 검증 조건). 허브의 `.1`은 되는데
      상대 기기가 안 되면 3절의 두 항목을 다시 보세요.
- [ ] **한쪽이 NAT/CGNAT 뒤(예: LTE 테더링)여도 된다** — 허브-스포크가
      "항상 된다"고 주장하는 근거입니다.
- [ ] **`PersistentKeepalive = 25`가 실제로 매핑을 유지한다** — 몇 분 두었
      다가 반대편에서 먼저 ping이 가는지.

## 6. 제거와 재가입

- [ ] **`sudo anago rm <이름>`(허브)이 상태와 wg 설정을 함께 갱신한다**
      `wg show`에서 피어가 사라지는지. 갱신 실패 시 "상태는 바뀌었지만
      인터페이스는 아직"이라고 정직하게 말하는지.
- [ ] **`anago rm <이름>`(기기, sudo 없이)이 API로 동작한다**
- [ ] **제거된 기기의 토큰이 즉시 죽는다** — 그 기기에서 `anago ls`가 401을
      받고, 안내대로 정리(`wg-quick down` → 두 파일 삭제 → 새 코드로 재join)
      하면 다시 붙는지.
- [ ] **비운 주소가 재사용된다** — 다음 join이 `.2`를 다시 받는지.

## 7. 플랫폼 차이 (맥 ↔ 리눅스)

- [ ] **맥에서 `wg-quick up`이 동작한다** (`brew install wireguard-tools`).
      리눅스는 커널 모듈, 맥은 유저스페이스(wireguard-go) — 인터페이스
      이름과 라우팅 동작이 다를 수 있습니다.
- [ ] **ping 타임아웃 플래그가 두 OS에서 맞다** — 리눅스 `-W`는 초, 맥
      `-W`는 밀리초라 anago는 맥에서 `-t`를 씁니다. 확인 단계가 **너무 빨리
      실패하지 않는지** 보세요.
- [ ] **맥에서 `ip_forward` 감지는 동작하지 않는다** — `/proc`이 없어
      anago는 조용히 넘어갑니다(추측하지 않음). 맥을 허브로 쓸 일은
      드물지만, 그 경우 `sysctl net.inet.ip.forwarding`을 직접 봐야 합니다.

## 8. 재부팅

- [ ] **VPS 재부팅 후 기기들이 다시 붙는다** — systemd 유닛, 방화벽 규칙,
      `ip_forward`, wg 인터페이스가 모두 살아 돌아오는지. 이 항목 하나가
      3절 전체의 실질적인 검증입니다.

---

# M1

여기부터는 **실 CA·실 zone·폰**이 있어야 알 수 있는 것들입니다.

**시작 상태를 먼저 읽으세요.** M1은 M0처럼 한 허브를 계속 이어 가며 볼 수
없습니다 — `server init`은 이미 선 허브를 보면 `AlreadyInitialized`로
**ACME에 닿기도 전에** 멈추고, HTTP-01·DNS-01·수동 인증서는 애초에 서로
다른 허브 상태입니다. 그래서 10절에 시나리오를 나눠 두었고, 절마다
"시작 상태"를 적었습니다. 9절은 10절을 돌리기 **전에** 준비하고 돌리는
**동안** 확인할 것들이고, 13~16절은 인증서가 선 허브면 어디서든 됩니다.

**버릴 수 있는 환경을 쓰세요** — 스냅샷을 되돌릴 수 있는 VPS가 가장
편합니다. 아래 리셋은 허브와 등록된 기기를 **전부 버립니다**:

```sh
sudo systemctl disable --now anago
sudo rm -rf /var/lib/anago /etc/wireguard/anago.conf
sudo rm -f /etc/systemd/system/anago.service
sudo systemctl daemon-reload
```

`server init`이 보는 것은 `state.json`과 `/etc/wireguard/anago.conf` 둘
뿐이라 이 둘만 없으면 처음부터 다시 섭니다. 기기 쪽도
`sudo wg-quick down anago` 후 `device.json`과 wg 설정을 지우고 새 코드로
다시 join해야 합니다.

## 9. M1 준비 — Cloudflare 토큰과 :80

- [ ] **토큰에 권한이 둘 다 있다** — **Zone → DNS → Edit**(레코드 쓰기)과
      **Zone → Zone → Read**(도메인이 어느 zone인지 찾기). 읽기 권한이
      없는 토큰은 `verify`를 통과하고 레코드 쓰기 권한도 있는데 **zone
      조회에서** 실패합니다. Edit만 준 토큰으로 한 번 돌려 보세요 —
      Cloudflare가 403을 주면 anago가 두 권한을 다 이름으로 대야 하고,
      빈 목록을 주면(스코프 문제와 구별되지 않습니다) 어느 계정의
      대시보드를 보고 무엇을 넓혀야 하는지 말해야 합니다. **무엇이
      부족한지 말하지 않는 메시지가 나오면 그 자체가 실패 항목입니다.**
- [ ] **토큰 파일이 0600이다** — 아니면 anago가 경고 한 줄을 찍고 계속
      진행합니다. 일부러 `chmod 644`로 한 번 확인하세요. 그 토큰은 zone
      전체를 고칠 수 있습니다.
- [ ] **폰에서 ICMP를 보낼 도구가 준비돼 있다** — 16절의 합격 기준이
      **양쪽 다 ping**이기 때문입니다. iOS에는 기본 `ping`이 없고 안드로이드
      도 기본 셸이 없는 기기가 흔하므로, 네트워크 진단 앱이나 터미널 앱을
      **미리** 깔아 두세요. 폰이 붙은 뒤에 없는 걸 알면 그 자리에서
      기준을 낮추고 싶어집니다.
- [ ] **HTTP-01 경로라면 A 레코드가 `server init` *전에* 이미 이 서버로
      해석된다** — CA가 밖에서 `http://<도메인>/.well-known/...`을 가져가
      보기 때문입니다. init은 인증서를 받은 **다음에** 안내를 출력하므로,
      레코드가 없으면 안내를 보기도 전에 발급에서 멈춥니다. `dig +short`로
      먼저 확인하세요.
- [ ] **HTTP-01 경로라면 :80이 밖에서 열려 있다** — 그리고 **열린 채로
      남는다**. anago는 챌린지 순간에만 :80을 잡으므로 평소에 포트를
      스캔해도 확인되지 않습니다. 확인은 둘 중 하나:
      1. 규칙을 눈으로 — 클라우드 보안 그룹의 인바운드 TCP 80과 호스트
         방화벽 양쪽.
      2. 서버에서 잠깐 리스너를 띄우고(`sudo python3 -m http.server 80`)
         밖에서 `curl -sv http://<도메인>/`.

      **첫 발급만 통과시키고 닫는 것이 이 항목의 함정입니다** — 갱신은
      몇 주 뒤에 조용히 실패하고, 그때는 인증서가 만료돼서야 압니다.
      8절의 재부팅 확인처럼 **규칙이 저장돼 있는지**까지 보세요.

## 10. ACME — 발급 (스테이징 → 프로덕션)

**시작 상태: 리셋 직후의 빈 기계.** 최초 발급과 "실패해도 아무것도 안
남는다"는 여기서만 볼 수 있습니다. 시나리오 셋은 서로 다른 허브 상태라
각각 리셋에서 시작하고, **순서가 있습니다**:

```
10-A (HTTP-01) → 리셋 → 10-C (수동 인증서) + 11절의 핫 리로드 항목
              → 리셋 → 10-B (DNS-01) → 11절 나머지 → 12~16절
```

**10-B가 마지막인 이유**는 12절부터 16절까지가 그 허브(토큰이 있고
DNS-01로 발급된)를 그대로 쓰기 때문입니다. 10-C를 뒤에 두면 리셋이
12절의 시작 상태를 지워 버립니다.

- **10-A. HTTP-01 최초 발급** — 토큰 없이 `server init --acme-email …`.
  A 레코드가 **미리** 서 있어야 하고 :80이 열려 있어야 합니다(9절).
- **10-C. 수동 인증서** — `--tls-cert/--tls-key`. ACME는 돌지 않습니다.
  11절의 마지막 항목(핫 리로드)을 **리셋 전에 여기서** 확인하세요.
- **10-B. DNS-01 최초 발급** — 토큰과 함께. A 레코드도 anago가 만듭니다.

**A 레코드는 리셋으로 지워지지 않습니다.** 로컬 리셋은 이 기계의 파일만
지우고, Cloudflare의 레코드는 그대로 남습니다. 게다가 **실패한 init도 A
레코드는 남깁니다** — 만들어 둔 레코드를 나가는 길에 지우면 멀쩡히 돌던
도메인을 망가뜨리므로 일부러 그렇게 돼 있습니다(§6.1). 그래서 10-B가
"created the A record"를 찍게 하려면 둘 중 하나:

- 10-A·10-C는 **다른 호스트 이름**으로 돌린다(예: `t1.example.com`), 또는
- 10-B 직전에 대시보드에서 그 이름의 A 레코드를 **지운다**.

그러지 않으면 anago는 있는 레코드를 adopt하거나 unchanged로 처리하고,
"created the A record" 대신 다른 줄이 나옵니다 — 그것도 정상 동작이지만
이 절이 보려는 것은 아닙니다.

**이미 선 M0 허브밖에 없다면**: `server renew`가 그 허브를 anago 발급으로
**전환**합니다. 계정 등록·챌린지·발급까지 최초 발급과 같은 코드를 타므로
아래 항목 대부분을 그대로 볼 수 있고, 스테이징 → 프로덕션도 같은 순서로
확인합니다. 다만 **첫 명령에 `--acme-staging`을 넣어야** 합니다 — 안
넣으면 곧장 프로덕션으로 발급돼 스테이징 항목 둘(`UnknownIssuer` 확인과
프로덕션 전환)을 건너뛰게 됩니다:

```sh
# 1. 전환하면서 스테이징으로 (HTTP-01)
sudo anago server renew --acme-email you@example.com --acme-staging
# DNS-01로 보려면 챌린지도 함께 — 토큰만 주면 anago가 거절합니다
#   sudo anago server renew --acme-email you@example.com --acme-staging \
#     --acme-challenge dns-01 --cf-token-file /root/cf-token

# 2. 이 상태에서 `anago join`이 UnknownIssuer로 실패하는지 확인

# 3. 프로덕션으로. CA를 바꾸는 플래그라 --force 없이 바로 재발급됩니다
sudo anago server renew --acme-production
```

이 경로에서 볼 수 없는 것은 **"실패가 아무것도 남기지 않는다"** 하나입니다
— 이미 허브가 서 있어서, 그 항목이 확인하려는 상태가 존재하지 않습니다.

- [ ] **`--acme-staging`으로 끝까지 간다** — `server init`이 "issued a
      certificate ... via http-01/dns-01"과 만료·갱신 두 날짜를 찍고,
      그 아래 **스테이징 경고**가 join 안내보다 **먼저** 나오는지.
- [ ] **그 상태에서 `anago join`이 `UnknownIssuer`로 실패한다** — 실패가
      **정상**입니다. 이걸 확인해야 다음 항목이 의미가 있습니다(스테이징
      인증서는 아무도 믿지 않습니다).
- [ ] **`sudo anago server renew --acme-production`이 즉시 재발급한다** —
      `--force` 없이. CA를 바꾸는 플래그는 그 자체로 재발급입니다.
- [ ] **서빙되는 인증서가 재시작 없이 바뀐다** — 허브를 건드리지 말고
      밖에서:

      ```sh
      echo | openssl s_client -connect net.example.com:443 \
        -servername net.example.com 2>/dev/null |
        openssl x509 -noout -issuer -dates
      ```

      issuer가 `(STAGING) ...`에서 진짜 Let's Encrypt로 바뀌고 `notBefore`가
      방금인지. 프로세스를 재시작해야 바뀐다면 §9.1의 핫 리로드가 안 도는
      것입니다.
- [ ] **그때부터 `anago join`이 성공한다.**
- [ ] **발급물이 제자리에 0600으로 있다** —
      `sudo ls -l /var/lib/anago/tls/` 의 `fullchain.pem`, `privkey.pem`,
      `account.key`. `account.key`는 이 허브가 CA에게 자기를 증명하는
      자격이라 특히 봐야 합니다.
- [ ] **실패가 아무것도 남기지 않는다** (**리셋 직후에만**) — 실패시키는
      조건은 챌린지마다 다릅니다. **DNS-01은 :80도 A 레코드도 쓰지 않으므로
      그 둘을 건드려 봐야 그냥 발급됩니다.**
      - **10-A(HTTP-01)**: :80을 닫고 `server init`. CA의 검증에서
        멈추므로 **ACME 단계까지 갔다가** 되돌아오는 경로 — `account.key`와
        인증서 쌍이 지워지는 것까지 여기서 확인됩니다.
      - **10-B(DNS-01)**: TXT를 쓸 수 없게 만듭니다. 가장 간단한 것은
        **Zone → Zone → Read만 있고 DNS → Edit이 없는 토큰**입니다. anago는
        zone은 찾고 레코드 쓰기에서 멈추므로, 이쪽은 **CA에 닿기 전에**
        끝납니다 — 그래서 ACME 롤백은 10-A가 확인하고, 이 항목은 "파일이
        안 생긴다"만 봅니다.

      **디렉터리가 없어야 하는 게 아닙니다.** init은 잠금을 잡기 전에
      `/var/lib/anago`(0700)를 만들고, `state.lock`은 풀린 뒤에도 그대로
      둡니다 — 다른 실행이 열어 둔 잠금 파일을 지우면 둘 다 잠갔다고
      믿게 되니까요. 계정을 등록한 데까지 갔다면 `tls/`도 만들어졌다가
      **속만 비워진 채** 남습니다. 볼 것은 **허브의 산출물이 없는지**입니다:

      ```sh
      sudo find /var/lib/anago -mindepth 1
      sudo ls -l /etc/wireguard/anago.conf
      ```

      첫 줄은 `state.lock`과 (있다면) 빈 `tls/`만 나와야 하고, 둘째 줄은
      없다고 답해야 합니다. `state.json`·`cf-token`·`tls/account.key`·
      `tls/*.pem` 중 하나라도 있으면 실패입니다.

      **남은 디렉터리가 재시도를 막지는 않습니다** — init이 "이미 섰다"고
      판정하는 것은 `state.json`과 `/etc/wireguard/anago.conf` 둘뿐이라,
      같은 명령을 다시 돌리면 그대로 진행됩니다. **A 레코드가 남는 것도
      정상**입니다(위 참조). 확인 뒤 조건을 되돌리고 리셋한 다음 제대로
      세우세요.
- [ ] **레이트 리밋 문구가 상황에 맞다**(걸렸다면) — 대기 시간과 함께,
      **디스크의 인증서가 그때까지 버티는지**를 말하는지. 갓 세운 허브(init)
      에서는 스테이징을 권하고, 돌고 있는 허브(renew)에서는 **권하지 않아야**
      합니다.

## 11. ACME — 갱신

**시작 상태: 10-B가 끝난 허브**(anago가 DNS-01로 발급한 인증서가 있는
상태). 마지막 항목 하나만 **10-C 차례에, 리셋 전에** 봅니다 — 수동
인증서가 있어야 하는 항목이라 여기서는 확인할 대상이 없습니다.

- [ ] **`sudo anago server renew`(플래그 없이)가 아직 아니라고 말하고
      아무것도 안 한다** — 토큰도 안 읽고 zone도 안 보고 파일도 안 씁니다.
      이게 "cron에 넣어도 안전"의 뜻입니다.
- [ ] **`sudo anago server renew --force`가 실제로 새 인증서를 받는다** —
      위 `openssl` 명령의 `notBefore`로 확인. (CA의 주간 한도를 쓰므로
      한 번만.)
- [ ] **주기 점검이 스스로 트리거된다** — 갱신 시점까지 몇 주를 기다릴 수는
      없으므로 결정을 앞당깁니다:

      ```sh
      sudo systemctl stop anago
      # tls.acme.renew_after 를 과거 시각으로 바꿉니다
      sudo $EDITOR /var/lib/anago/state.json
      sudo systemctl start anago
      journalctl -u anago -f
      ```

      허브는 **한 시간에 한 번**(`CHECK_EVERY`) 상태를 다시 읽으므로, 늦어도
      그 안에 갱신이 도는지 봅니다. §9.1이 만료가 아니라 **결정**(`renew_after`)
      을 파일에 적기로 한 덕에 이 확인이 가능합니다.
- [ ] **수동 인증서도 파일이 바뀌면 집어 든다** (**10-C**) — certbot이
      갱신하는 허브에서, 파일을 갈아끼운 뒤 한 시간 안에 `openssl` 결과가
      바뀌는지. anago가 발급하지 않은 인증서에 대해 anago가 하는 유일한
      일이고, 그래서 이 항목만 시작 상태가 다릅니다.

## 12. Cloudflare — A 레코드와 DNS-01

**시작 상태: 10-B**(토큰을 준 허브).

- [ ] **A 레코드가 자동으로 생긴다** — init 출력의 "created the A record,
      DNS only — no orange cloud (<id>)"와 대시보드의 실제 레코드가 맞는지,
      그리고 `dig +short`가 VPS 주소를 돌려주는지.
- [ ] **구름이 회색이다** — 대시보드에서 **눈으로**. 프록시가 켜져 있어도
      DNS-01은 TXT로 증명하므로 인증서는 발급되고 HTTPS도 됩니다. 화면의
      모든 줄이 초록인 채로 터널만 죽는 실패라, 자동화로는 절대 드러나지
      않습니다(§13).
- [ ] **이미 프록시가 켜진 레코드를 anago가 끄지 않고 거절한다** — 일부러
      주황 구름을 켜고 `sudo anago server renew --dns`. "will not turn that
      off ... set the record to DNS only (grey cloud)"로 멈춰야 합니다.

      **확인하고 바로 회색으로 되돌리세요.** anago는 자기가 켜지 않은
      프록시를 끄지 않으므로 켜 둔 채로 넘어가면 이름이 Cloudflare 주소로
      풀리고, 13~16절의 터널이 전부 실패합니다 — 그것도 "HTTPS는 되는데
      터널만 안 되는" 형태로. 되돌린 뒤 `dig +short`가 VPS 주소를 다시
      돌려주는지 확인하고 다음으로 가세요.
- [ ] **DNS-01 발급이 실제로 된다** — 토큰이 있는 허브에서 챌린지 기본값이
      dns-01인지, 전파 대기가 성공하는지. anago는 **zone의 권한
      네임서버에 직접** TXT를 묻습니다(로컬 리졸버 캐시를 믿지 않습니다).
- [ ] **anago가 만든 챌린지 TXT가 남지 않는다** — **RRset이 비었는지로
      판정하면 안 됩니다.** `_acme-challenge`에는 다른 ACME 실행이나
      와일드카드 주문이 만든 TXT가 **합법적으로 함께** 있을 수 있고,
      anago도 자기가 만든 레코드만 지우고 나머지는 그대로 둡니다. 비었기를
      기대하면 정상 동작을 실패로 적고, 남의 레코드를 지우게 만듭니다.

      볼 것은 **이번 실행이 만든 것이 사라졌는지**입니다. anago는 자기
      레코드의 코멘트에 표식을 답니다:

      ```
      anago DNS-01 challenge at=<유닉스 시각> pid=<프로세스 id>
      ```

      Cloudflare 대시보드에서 `_acme-challenge.<도메인>`의 TXT 목록을 열어
      **그 코멘트가 붙은 레코드가 남아 있지 않은지** 보세요. 표식 없는
      TXT가 있다면 그것은 남겨 두는 것이 맞습니다.

      `dig`로 본다면 "비어야 한다"가 아니라 **발급 전후를 비교**해서 값이
      원래대로 돌아왔는지 봅니다. 정리가 실패한 경우 anago가 **어느
      레코드인지 id로** 말했어야 하므로, 지울 때도 그 id만 지우세요.
- [ ] **토큰이 상태 파일에 없다** — 경로만 있어야 합니다:

      ```sh
      sudo ls -l /var/lib/anago/cf-token           # 0600
      sudo grep -c '<토큰 앞 8자>' /var/lib/anago/state.json   # 0 이어야 합니다
      ```
- [ ] **전파 타임아웃이 정직하다**(재현되면) — TXT가 끝내 안 서면 anago는
      **CA에게 "준비됐다"고 말하지 않고** 멈춥니다. 아끼는 것은 **실패한
      인증 시도**(셋업 중에 걸리기 가장 쉬운 한도)이지 CA 호출 전체가
      아닙니다 — 주문(new-order)은 챌린지 값을 받으려고 그보다 **먼저**
      만들어지므로 그 한도는 이미 쓴 뒤입니다. 문구가 그 경계대로 말하는지
      보세요. 레코드 처리도 "지웠다"가 아니라 **나가면서 뺀다**여야
      합니다.

## 13. 폰 합류 — QR

- [ ] **`anago join <도메인> <코드> --export qr --name 폰`이 sudo 없이
      된다** — 이 경로는 로컬에 아무것도 쓰지 않고 인터페이스도 올리지
      않습니다.
- [ ] **정말 아무것도 안 남는다** — 실행한 기계에서
      `ls ~/.config/anago/`와 `sudo ls -l /etc/wireguard/`를 실행 전후로
      비교. 폰 몫의 파일이 생기면 안 됩니다.
- [ ] **QR이 온전히 그려지고 폰 앱이 읽는다** — 공식 WireGuard 앱의
      **+ → QR 코드에서 만들기**. 창이 좁으면 anago가 **그리지 않고
      거절**해야 합니다(줄바꿈된 QR도 QR처럼 보이므로). 일부러 창을 좁혀
      한 번 확인하세요.
- [ ] **경고와 정리 안내가 QR 다음에, 표준오류로 나온다** — 설정 본문은
      표준출력, 나머지는 표준오류라 `--export conf > phone.conf`가 설정만
      담습니다. 안내가 **화면과 기록 두 단계**를 순서대로 말하는지도.
- [ ] **`--export conf --out <경로>`가 0600으로 쓰고, 이미 있는 파일은
      덮어쓰지 않고 에러다** — 같은 경로로 두 번 실행해 확인.
- [ ] **폰이 허브 상태에 보통 기기로 들어간다** — `anago ls`에 이름·주소·
      공개키가 뜨는지.
- [ ] **폰을 `anago rm`으로 지울 수 있다 — 허브에서도, 합류한 기기에서도**.
      폰 자신만 못 합니다(토큰이 없으니까요). 지운 뒤 폰의 터널이 죽는지도
      확인하세요.

      **확인했으면 새 코드로 다시 붙이세요.** 16절의 합격 기준이 그 폰을
      씁니다 — 지운 채로 두면 확인할 대상이 없습니다. (같은 이름으로 다시
      `--export qr` 하면 됩니다. 폰 앱에서는 옛 터널을 지우고 새 QR을 다시
      스캔해야 합니다 — 키가 새로 만들어지므로 옛 프로필은 못 씁니다.)

## 14. sync 타이머

- [ ] **join하지 않은 기계에서 `--install-timer`가 거절한다** — 가리킬
      `device.json`이 없는 타이머는 설치된 것처럼 보이면서 몇 분마다
      실패하기만 합니다.
- [ ] **설치되고 켜진다**

      ```sh
      # 리눅스
      systemctl list-timers 'anago-sync*'
      systemctl cat anago-sync.service

      # 맥 — sudo와 `system/` 둘 다 필요합니다
      sudo launchctl print system/com.github.elpalaiso.anago.sync
      ```
- [ ] **유닛이 가리키는 `device.json`이 맞다** — 위 출력의 `--config`
      경로가 **가입한 사용자의** 파일인지. root의 `/root/.config/...`를
      가리키면 그 타이머는 영영 아무것도 못 합니다.
- [ ] **실제로 주기적으로 돈다** — 설치된 유닛은 `--quiet`로 돌기 때문에
      **정상 실행은 아무것도 출력하지 않습니다.** 그러니 로그가 비었다고
      안 돈 것이 아닙니다. 확인은:
      - 리눅스: `systemctl list-timers 'anago-sync*'`의 **LAST/PASSED**가
        주기마다 갱신되는지. 유닛 자체의 시작·종료는 프로그램이 조용해도
        `journalctl -u anago-sync.service`에 남습니다.
      - 맥: `sudo launchctl print system/com.github.elpalaiso.anago.sync`의
        **`runs`**가 늘고 `last exit code = 0`인지.
        `/var/log/anago-sync.log`는 **문제가 있을 때만** 자랍니다.
- [ ] **그 실행이 일을 한다** — 가장 확실한 증거입니다. 허브에서 그 기기를
      `sudo anago rm <이름>` 하고 다음 주기를 기다리세요. sync가 401을 받고
      **"제거됐다"고 말하며 멈춰야** 하고, **터널을 스스로 내리면 안
      됩니다** — 몇 분마다 도는 무인 작업이 터널을 내리지 않는다는 것이
      §6.3의 규칙입니다. 정리는 사람에게 안내로 넘어옵니다. (이 항목 뒤에는
      그 기기가 빠진 상태이므로, 아래를 이어서 보려면 새 코드로 다시
      join하세요.)
- [ ] **네트워크가 없을 때 조용히 실패한다** — 기기를 오프라인으로 두고
      한 주기 보내세요. 허브에 못 닿는 것은 **exit 0**이어야 합니다
      (카페 네트워크에서 하루를 보낸 노트북이 실패 유닛 수백 개를 쌓으면,
      정작 읽어야 할 하나가 묻힙니다).
- [ ] **`--uninstall-timer`가 파일도 지우고 로드도 푼다** — 위 확인 명령이
      "없다"고 답하는지. 로드를 못 풀었으면 anago가 그렇게 **말해야**
      합니다(파일만 지우고 성공했다고 하면 안 됩니다).
- [ ] **주기를 바꾸려면 떼고 다시 설치한다** — `--interval 1h`로 다시 깔고
      유닛의 값이 바뀌었는지.
- [ ] **systemd도 launchd도 없는 기계**(있다면) — 설치하지 않고 crontab
      줄을 출력하는지, 그리고 `--interval 7m`처럼 cron이 지킬 수 없는
      주기는 거절하는지.
- [ ] **놓친 실행은 따라잡지 않는다** — 노트북을 재웠다 깨우면 systemd
      타이머는 밀린 실행을 몰아서 돌리지 않고 다음 주기에 돕니다
      (`Persistent=` 없음, 의도된 것). 깨운 직후 폭주하지 않는지만 보세요.

## 15. 재부팅 — M1 몫

- [ ] **VPS 재부팅 후 인증서가 그대로 서빙된다** — 위 `openssl` 명령이
      같은 인증서를 돌려주는지, `journalctl -u anago`에 갱신 루프가 다시
      뜨는지.
- [ ] **기기 재부팅 후 타이머가 스스로 돌아온다** — 리눅스는
      `systemctl is-active anago-sync.timer`, 맥은 `launchctl print`.
- [ ] **맥: 시스템 설정 → 로그인 항목 및 확장 프로그램에서 꺼져 있지
      않은지** — 여기서 끄면 설치는 멀쩡한 채로 한 번도 실행되지 않고,
      파일만 봐서는 구별되지 않습니다.

## 16. 실제로 통하는지 — M1의 합격 기준

- [ ] **폰 ↔ 기존 기기 상호 ping** (DESIGN §11). **양쪽 다 ICMP로**
      확인합니다. 두 번 하는 이유는 한 방향의 왕복이 이미 양쪽 경로를 다
      지나기 때문입니다 — 한 번의 성공이 증명하지 못하는 것은 **반대쪽이
      새 흐름을 먼저 시작할 수 있는지** 하나뿐입니다. 한쪽만 되면 통과가
      아니라 허브의 FORWARD 정책 문제입니다(3절).
      - **노트북 → 폰**: `ping <폰 주소>` (주소는 `anago ls`에 있습니다).
      - **폰 → 노트북**: 9절에서 준비한 ICMP 도구로 `<노트북 주소>`를
        칩니다.

      안 되면 노트북 쪽 방화벽이 ICMP를 막고 있지 않은지 먼저 보세요 —
      허브의 FORWARD 문제와 증상이 같습니다. 잘라 보는 데는 HTTP 왕복
      (노트북에서 `python3 -m http.server 8000`, 폰 브라우저로
      `http://<노트북 주소>:8000`)이 편하지만, **그것이 되는 것으로 이
      항목을 통과 처리하지는 마세요** — 합격 기준은 ping입니다. 폰에서
      ICMP를 끝내 못 보내는 기기라면 이 항목을 **막힘**으로 적고 기준
      자체를 계획 단계에서 정하도록 올리세요(DESIGN §11).
- [ ] **그 인증서가 프로덕션이다** — 스테이징으로 통과시키면 폰 앱은 상관
      없지만 `anago join`이 안 되므로, 합격은 프로덕션 인증서 상태에서
      확인합니다.
- [ ] **타이머가 돈 흔적이 있다** — 14절의 "실제로 일을 한다" 항목까지.

---

# M1.5 — 윈도우·맥 허브

**검증 환경**: 소유자의 실환경 그대로 — 상주 윈도우 PC(이중 NAT 공유기
뒤) + 유목 맥북 + 폰. 이 절이 통과하면 "VPS 없이 집 PC가 허브"라는
M1.5의 존재 이유가 검증된 것이다. 코드 어디에도 윈도우 실기기가 없었으
므로, 아래는 전부 사람 몫이다.

## 17. 윈도우 허브 — 준비

- [ ] **공식 WireGuard 클라이언트가 설치돼 있다** — `winget install
      WireGuard.WireGuard`. anago는 wg.exe와 wireguard.exe를 PATH에서,
      없으면 `%ProgramFiles%\WireGuard`에서 찾는다 — **PATH에 없어도
      찾는지**가 확인 지점이다(설치기는 PATH를 건드리지 않는다).
- [ ] **공유기 포트포워딩** — 51820/udp·443/tcp를 이 PC로. 이중 NAT면
      위층 장비에서 DMZ(또는 같은 포워딩)를 아래층 공유기로. `dig`로
      도메인이 집 공인 IP를 가리키는지 먼저 확인.
- [ ] **관리자 PowerShell에서 실행한다** — 일반 셸이면 서비스 생성과
      %ProgramData% 쓰기에서 접근 거부가 나야 하고, **그 에러가 관리자
      로 다시 실행하라고 말하는지**도 본다(문구도 기능이다, §8).

## 18. 윈도우 허브 — 세우기와 상주

- [ ] **`anago server init --domain … --cf-token-file …`이 끝까지 간다**
      — 상태가 `%ProgramData%\anago`에, wg 설정이 그 옆에 만들어지고,
      터널 서비스(`wg show anago`)와 윈도우 서비스(`sc query anago`)가
      둘 다 선다. 출력의 netsh 방화벽 안내 2줄을 실행한다.
- [ ] **`Set-NetIPInterface -InterfaceAlias anago -Forwarding Enabled`**
      — 피어 중계에 필수. anago는 윈도우에서 이를 감지하지 못하므로
      (설계상 안내만) 출력이 시키는 대로 했는지가 곧 검증이다.
- [ ] **재부팅 후 둘 다 돌아온다** — 서비스(`sc query anago`)와 터널
      (`wg show anago`), 그리고 기기들이 다시 붙는다. 3절의 재부팅
      검증에 대응하는 윈도우 판.
- [ ] **`sc stop anago` 후 상태 파일이 멀쩡하다** — 서비스 정지는 즉시
      종료다(winsvc 설계: 원자적 쓰기라 안전). 정지 → 시작 후 `anago
      ls`가 이전 기기들을 그대로 보여주는지.
- [ ] **실패 로그가 남는다** — 인증서 경로를 일부러 틀린 뒤 `sc start
      anago`: 서비스는 죽고 `%ProgramData%\anago\service.log`에 이유가
      적혀야 한다(서비스에는 콘솔이 없다).

## 19. 윈도우 허브 — 기기 관점

- [ ] **맥북이 카페 와이파이/LTE에서 join·ping** — 집 공유기 밖에서.
      허브-스포크가 "항상 된다"의 가정용 판.
- [ ] **폰이 QR로 붙고, 맥북 ↔ 폰 상호 ping** — 16절과 같은 기준,
      허브만 집 PC로 바뀐 것.
- [ ] **공인 IP가 바뀌면** (재현 가능하면) — 통신사 DHCP 갱신 후
      `anago server renew --dns`가 A 레코드를 새 IP로 밀고, 기기들이
      다음 핸드셰이크에서 따라오는지.

## 20. 맥 허브 (맥미니가 있다면)

- [ ] **`sudo anago server init …`이 LaunchDaemon까지 세운다** —
      `/Library/LaunchDaemons/com.github.elpalaiso.anago.plist`,
      `sudo launchctl print system/com.github.elpalaiso.anago`.
- [ ] **재부팅 후 데몬·터널·포워딩이 돌아온다** — 특히 포워딩: 맥은
      sysctl.conf가 없어 **데몬이 기동 시 재설정**한다(§11.1 결정 5).
      재부팅 → 기기 2대 상호 ping이 곧 검증이다.
- [ ] **로그가 `/var/log/anago.log`에 쌓인다.**

---

## 확인되면

1~8절이 통과하면 DESIGN §11의 **M0 행**이, 9절부터도 통과하면 **M1 행**이
"구현 완료, 미검증"에서 검증 완료로 바뀌고, README의 상태 절도 함께
갱신됩니다. 두 행은 따로 넘어갑니다 — M0만 통과한 상태도 정직한
상태입니다. 다만 순서는 M0가 먼저입니다: M1의 양방향 도달은 3절의 FORWARD
정책이 맞아야 성립하므로, M1 행만 먼저 넘길 수는 없습니다.

실패한 항목은 그 자체로 다음 작업 목록입니다 — 문서를 고칠 일인지 코드를
고칠 일인지 함께 적어 주세요. **안내 문구가 틀린 것도 실패로 적어
주세요**: 이 프로젝트에서 메시지는 장식이 아니라 기능이고(§8), 사람이
한 번 돌려봐야만 드러나는 종류의 오류가 여기 모여 있습니다.
