# 사람 확인 필요 — M0

M0는 **코드가 완성됐지만 실서버에서 검증되지 않았습니다**. 유닛/통합
테스트(약 420개)는 결정 가능한 부분만 덮습니다 — 어떤 설정을 만들지, 어떤
상태 전이를 할지, 무엇을 거절하고 뭐라고 안내할지. 나머지, 즉 **커널·네트
워크·다른 기계가 있어야 알 수 있는 것**은 사람이 한 번 돌려봐야 합니다.

이 문서는 그 목록입니다. 소스에서 `**Human verification needed**`로 표시한
지점(16곳)과 리뷰에서 나온 운영상 함정을 모았습니다.

**검증 환경**(DESIGN §11): Cloudflare에 등록된 개인 도메인 + 리눅스 VPS
+ 기기 2대(맥 1, 리눅스 1이면 §5의 플랫폼 차이까지 같이 확인됩니다).

---

## 1. 준비 — DNS와 인증서

- [ ] **A 레코드가 VPS 공인 IP로 해석된다**
      `dig +short net.example.com` 이 VPS의 주소를 그대로 반환하는지.
      Cloudflare라면 그 레코드가 **DNS only(회색 구름)** 인지 — 프록시가
      켜져 있으면 이름이 Cloudflare 주소로 풀리고 `51820/udp`가 전달되지
      않습니다. HTTPS는 되는데 터널만 안 되는 형태로 실패합니다.
- [ ] **`server init`이 안내한 A 레코드가 실제 주소와 일치한다**
      init은 UDP 소켓의 로컬 주소로 공인 IP를 추정합니다(패킷은 보내지
      않습니다). AWS/GCP처럼 1:1 NAT를 쓰면 사설 주소가 나오고, 그 경우
      anago는 주소를 지어내지 않고 자리표시자를 출력해야 합니다 —
      **어느 쪽이 나왔는지 확인**하세요.
- [ ] **TLS 인증서가 기기에서 신뢰된다**
      공개 신뢰 인증서(Let's Encrypt 등)면 그대로. Cloudflare Origin CA를
      썼다면 **합류할 모든 기기에 그 CA를 설치**해야 하고, 안 하면 join이
      `UnknownIssuer`로 실패합니다.

## 2. 허브 세우기

- [ ] **`sudo anago server init …`이 끝까지 간다**
      실행되는 것: `wg genkey`/`wg pubkey`, `/var/lib/anago` 0700 생성과
      `state.json` 0600, `/etc/wireguard/anago.conf` 0600, systemd 유닛
      설치·기동. 출력의 A 레코드·포트 체크리스트·조인 코드 한 줄 확인.
- [ ] **두 번째 `server init`이 거부된다** — 기존 허브를 재키잉하지 않고
      멈추는지. (`state.json`이나 `anago.conf`가 이미 있는 경우)
- [ ] **systemd 유닛이 실제로 뜬다**
      `systemctl status anago`, `journalctl -u anago`. 유닛은
      `ProtectHome=tmpfs` + `BindReadOnlyPaths=`(바이너리·인증서·키)로
      샌드박스되어 있습니다 — **인증서가 `/root`나 `/tmp`에 있는 배치도
      기동되는지**가 이 항목의 핵심입니다.
- [ ] **`--no-systemd`면 `sudo anago server run`이 포그라운드로 뜬다**
      배너에 도메인·포트·기기 수가 나오는지. :443 바인드는 root가 필요합
      니다.
- [ ] **인증서 경로가 틀렸을 때 아무것도 만들지 않고 멈춘다**
      init은 파일을 쓰기 전에 TLS를 검증합니다. 실패 후 `state.json`이
      생기지 않았는지 확인 — 생겼다면 재시도가 막힙니다.

## 3. 방화벽과 포워딩 (기기끼리 안 닿는 원인 1·2위)

- [ ] **:443/tcp가 열려 있다** — 밖에서 `nc -vz <domain> 443`. 클라우드
      보안 그룹과 호스트 방화벽 **양쪽**을 봐야 합니다.
- [ ] **:51820/udp가 열려 있다** — 이건 포트 스캔으로 확인되지 않습니다.
      `nc -vzu`는 데이터그램을 보낸 것만으로 성공을 표시하고, WireGuard는
      인증되지 않은 패킷에 아무 응답도 하지 않으므로 **허용된 경우와
      드롭된 경우가 똑같이 보입니다**. 합격 증거는 둘입니다:
      1. 규칙 자체를 눈으로 확인 — 클라우드 보안 그룹의 인바운드 UDP
         51820, 그리고 호스트 쪽 `sudo ufw status verbose` /
         `sudo iptables -S` / `sudo nft list ruleset`.
      2. **실제 핸드셰이크가 성사됨** — 기기를 붙인 뒤 서버에서
         `sudo wg show anago` 의 `latest handshake`가 갱신되는지(기기
         쪽에서도 같은 명령). 값이 계속 비어 있으면 UDP가 막힌 것입니다.
      막힌 것 같은데 원인이 애매하면 서버에서
      `sudo tcpdump -ni any udp port 51820` 를 띄워 두고 기기에서 ping을
      보내 보세요 — 패킷이 아예 안 보이면 경로/보안그룹, 들어오는데 응답이
      없으면 호스트 방화벽이나 wg 설정입니다.
- [ ] **`net.ipv4.ip_forward=1`이고 재부팅 후에도 유지된다**
      (`/etc/sysctl.d/99-anago.conf`). 꺼져 있으면 anago가 적용 후 경고를
      찍는지도 함께 확인하세요.
- [ ] **FORWARD 정책이 `anago`→`anago`를 허용한다**
      UFW/iptables/nftables 중 쓰는 것 하나로. **재부팅 후에도 남는지**가
      핵심입니다 — anago는 systemd가 되살리지만 방화벽 규칙은 저장하지
      않으면 사라집니다.

## 4. 기기 등록

- [ ] **`sudo anago code` → `sudo anago join …`이 성공한다** (기기 1)
      기기가 키쌍을 만들고 공개키만 보내는지, wg 설정이 써지고 터널이
      올라오는지, 서버 `.1` ping 확인이 통과하는지.
- [ ] **sudo로 join했는데 이후 명령은 sudo 없이 된다**
      `anago ls`가 일반 사용자로 동작해야 합니다. 확인:
      `ls -l ~/.config/anago/device.json` 소유자가 **본인**이고 0600인지.
      (`SUDO_UID` 기반 위치 결정 + 디스크립터 `fchown` 경로 — 이건 root로
      실행돼야만 도는 코드라 테스트가 덮지 못합니다.)
- [ ] **설정 디렉터리를 미리 안 만들었으면 안내하고 멈춘다**
      `mkdir -p "${XDG_CONFIG_HOME:-$HOME/.config}/anago"` 없이 join하면
      `NoConfigDir`로 멈추는지 — root가 남의 홈에 디렉터리를 만들지 않는
      설계입니다.
- [ ] **`XDG_CONFIG_HOME`을 옮겨 쓴다면 `sudo -E`로 join한 뒤 `anago ls`가
      같은 파일을 찾는다**
- [ ] **기기 2도 붙는다** — 주소가 `.3`으로 나가는지, `anago ls`(허브에서
      `sudo`)에 둘 다 보이고 마지막 핸드셰이크가 찍히는지.

## 5. 실제로 통하는지 — M0의 합격 기준

- [ ] **기기 1 ↔ 기기 2 상호 ping** (§11의 검증 조건). 허브의 `.1`은 되는데
      상대 기기가 안 되면 3절의 두 항목을 다시 보세요.
- [ ] **한쪽이 NAT/CGNAT 뒤(예: LTE 테더링)여도 된다** — 허브-스포크가
      "항상 된다"고 주장하는 근거입니다.
- [ ] **`PersistentKeepalive = 25`가 실제로 매핑을 유지한다** — 몇 분 두었
      다가 반대편에서 먼저 ping이 가는지.

## 6. 제거와 재가입

- [ ] **`sudo anago rm <이름>`(허브)이 상태와 wg 설정을 함께 갱신한다**
      `wg show`에서 피어가 사라지는지. 갱신 실패 시 "상태는 바뀌었지만
      인터페이스는 아직"이라고 정직하게 말하는지.
- [ ] **`anago rm <이름>`(기기, sudo 없이)이 API로 동작한다**
- [ ] **제거된 기기의 토큰이 즉시 죽는다** — 그 기기에서 `anago ls`가 401을
      받고, 안내대로 정리(`wg-quick down` → 두 파일 삭제 → 새 코드로 재join)
      하면 다시 붙는지.
- [ ] **비운 주소가 재사용된다** — 다음 join이 `.2`를 다시 받는지.

## 7. 플랫폼 차이 (맥 ↔ 리눅스)

- [ ] **맥에서 `wg-quick up`이 동작한다** (`brew install wireguard-tools`).
      리눅스는 커널 모듈, 맥은 유저스페이스(wireguard-go) — 인터페이스
      이름과 라우팅 동작이 다를 수 있습니다.
- [ ] **ping 타임아웃 플래그가 두 OS에서 맞다** — 리눅스 `-W`는 초, 맥
      `-W`는 밀리초라 anago는 맥에서 `-t`를 씁니다. 확인 단계가 **너무 빨리
      실패하지 않는지** 보세요.
- [ ] **맥에서 `ip_forward` 감지는 동작하지 않는다** — `/proc`이 없어
      anago는 조용히 넘어갑니다(추측하지 않음). 맥을 허브로 쓸 일은
      드물지만, 그 경우 `sysctl net.inet.ip.forwarding`을 직접 봐야 합니다.

## 8. 재부팅

- [ ] **VPS 재부팅 후 기기들이 다시 붙는다** — systemd 유닛, 방화벽 규칙,
      `ip_forward`, wg 인터페이스가 모두 살아 돌아오는지. 이 항목 하나가
      3절 전체의 실질적인 검증입니다.

---

## 확인되면

이 목록이 통과하면 DESIGN §11의 M0 행이 "구현 완료, 미검증"에서 검증
완료로 바뀌고, README의 상태 절도 함께 갱신됩니다. 실패한 항목은 그
자체로 다음 작업 목록입니다 — 문서를 고칠 일인지 코드를 고칠 일인지 함께
적어 주세요.
